<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zhihu.testviewdemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScrollFrescoActivity" />

        <activity android:name=".ClickSpannableActivity" />

        <activity android:name=".recycler.RecyclerADActivity" />

        <activity android:name=".sticknav.view.MixtapeVideoPlayActivity" />

        <activity android:name=".draghelper.DragActivity" />

        <activity android:name=".sticknav.StickNavActivity2" />

        <activity android:name=".recycler.RecyclerActivity2" />

        <activity android:name=".dialog.DialogActivity" />

        <activity android:name=".scroller.ScrollerActivity" />

    </application>

</manifest>